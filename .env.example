# Copy to .env (gitignored) and fill in. The consultant_engine CLI loads .env
# automatically; real environment variables take precedence over .env values.
#
# Required for the real (non-fake) LLM path — prose generation + the live macro agent.
# Get a key at https://console.anthropic.com/  (keep it secret; never commit .env).
ANTHROPIC_API_KEY=sk-ant-your-key-here
