[pytest]
# Put the repo root on sys.path so `consultant_engine` imports when pytest is run
# as the bare console script (CI does `pytest`, not `python -m pytest`, and only
# the latter adds the CWD automatically).
pythonpath = .
testpaths = tests
