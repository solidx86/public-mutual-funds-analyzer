# Phase 0 stub — proves `npx promptfoo eval` is wired up before any judge
# logic exists. There are no fixtures, no rubric, and no real test cases yet;
# Phase 2/3 of the plan replace this file's contents entirely. See:
#   docs/superpowers/plans/2026-07-06-track-a-prose-number-eval.md
#
# The `echo` provider needs no API key and makes no network call at eval
# time — it exists only so the harness has something to run against. It is
# NOT the judge model. Phase 3 pins the real judge (claude-sonnet-5) and
# wires in the frozen fixture corpus.
description: "Prose-number entailment eval (scaffold stub, no test cases yet)"

prompts:
  - "stub prompt, unused until Phase 3: {{input}}"

providers:
  - id: echo

tests: []
